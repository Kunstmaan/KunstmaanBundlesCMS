{% extends 'KunstmaanAdminBundle:Default:layout.html.twig' %}

{% block header %}{% endblock %}

{% block content %}
<form method="post" action="">
    <h2>{{ 'dashboard.ga.setup.title' | trans }}</h2>
    <h3>{{ 'dashboard.ga.setup.profile.title' | trans }}</h3>
    <p>{{ 'dashboard.ga.setup.profile.content' | trans }}</p>

    <select name="profiles" class="chzn-select">
        {% for profile in profiles %}
            <option value="{{ profile.id }}">{{ profile.name }}</option>
        {% endfor %}
    </select>
<br/>
    <button class="btn btn-primary" type="submit">{{ 'form.save' | trans }}</button>
</form>

{% endblock %}
