{% if adminlist.getDeleteUrlFor(item) %}
    <div id="sure-modal-{{ item.id }}" class="modal hide fade">
        <div class="modal-header">
            <button class="close" data-dismiss="modal">&times;</button>
            <h3>Are you sure you want to delete this item?</h3>
        </div>
        <div class="modal-footer">
            <div class="btn_group">
                <a href="{{ path(adminlist.getDeleteUrlFor(item)["path"], adminlist.getDeleteUrlFor(item)[("params")] ) }}" class="btn btn-danger">{{ 'form.delete' | trans }}</a>
                <button class="btn" data-dismiss="modal">Cancel</button>
            </div>
        </div>
    </div>
{%  endif %}
