{% set action = itemAction.getUrlFor(item) %}
<a href="{{ path(action['path'], action['params']) }}">
    {% if item.isMark() %}
        <i class="fa fa-toggle-on" aria-hidden="true"></i> Unmark
    {% else %}
        <i class="fa fa-toggle-off" aria-hidden="true"></i> Mark
    {% endif %}
</a>