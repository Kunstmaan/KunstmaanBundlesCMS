{% extends 'KunstmaanAdminBundle:Settings:index.html.twig' %}

{% block subbreadcrumb %}
	<li><a href="{{ path('KunstmaanAdminBundle_settings') }}">{{ 'settings.title' | trans }}</a></li>
	<li class="active">{{ 'Searches' | trans }}</li>
{% endblock %}

{% block content %}

    <div class="page-header">
        <h1>{{ 'Searches' | trans }}</h1>
    </div>

    {{ adminlist_widget(searchedforadminlist, 'KunstmaanAdminBundle_settings_searches') }}

{% endblock %}
