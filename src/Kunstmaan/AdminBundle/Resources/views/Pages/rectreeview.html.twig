<li class="{% if node.active %}jstree-open{% endif %} {% if nodemenu.current is not null and nodemenu.current.id == node.id  %}active{% endif %}" rel="page">
    <a href="{{ path('KunstmaanAdminBundle_pages_edit', { 'id': node.id }) }}">{{ node.title }}</a>
    <ul>
        {% for node in node.children %}
            {% include 'KunstmaanAdminBundle:Pages:rectreeview.html.twig' %}
        {% endfor %}
    </ul>
</li>