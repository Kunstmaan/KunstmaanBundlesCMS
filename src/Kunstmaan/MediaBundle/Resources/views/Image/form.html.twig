<form action="{{ path('KunstmaanKMediaBundle_image_create', { 'gallery_id' : gallery.id } ) }}" method="post" {{ form_enctype(form) }}>
    <fieldset>
        <div class="clearfix">
            {{ form_errors(form) }}
            <label for="xlInput3">Image</label>
            <div class="input">
                {{ form_widget(form.media) }}
            </div>
            {{ form_rest(form) }}
        </div>
        <div class="actions">
            <input class="btn primary type" type="submit" value="add new image">
            <input onclick="window.location = '{{ path('KunstmaanKMediaBundle_imagegallery_show', { 'id' : gallery.id, 'slug' : gallery.slug } ) }}'" type="reset" class="btn" value="cancel" />
        </div>
    </fieldset>
</form>