{% if pagepartref is defined %}
<script>
$(document).ready(function() {
    $('select.imagechooser').on('click', function(ev) {
        ev.preventDefault();
        window.open('http://omnext.perunu.kunstmaan.be/app_dev.php/en/admin/media/ckeditor?CKEditor=form_pagepartadmin10_content&CKEditorFuncNum=2&langCode=nl', 'width=300,height=200');
    });
});

window.ImagegalleryImageChosenCallback['{{ pagepartref.id }}'] = function(url, id) {
    $('select.imagechooser').val(id);
}
</script>
{% endif %}
<img src="{{ asset(upload_dir~resource) }}" />