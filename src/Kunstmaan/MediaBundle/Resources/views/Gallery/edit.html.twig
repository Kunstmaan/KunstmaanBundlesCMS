{% extends 'KunstmaanMediaBundle:Default:layout.html.twig' %}

{% block subsidebar %}
    {% include 'KunstmaanMediaBundle:Gallery:sidebar.html.twig' %}
{% endblock %}

{% block subbreadcrumb %}
    <li><a href="{{ path('KunstmaanMediaBundle_media_' ~ gallery.type ~ 's') }}">{{ gallery.type }}s</a> <span class="divider">/</span></li>
	<li class="active">{{ 'media.gallery.edit' |trans }}: {{ gallery.name }}</li>
{% endblock %}

{% block content %}

<div class="page-header">
    <h1>{{ 'media.gallery.edit' |trans }}: {{ gallery.name }}</h1>
  </div>
   {% include 'KunstmaanMediaBundle:Gallery:editform.html.twig' with { 'form': form } %}
{% endblock %}