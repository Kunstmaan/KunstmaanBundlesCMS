<div id="delete-modal" class="modal hide fade">
    <div class="modal-header">
        <a href="#" class="close">&times;</a>
        <h3>Are you sure you want to delete {{ gallery.name }}?</h3>
    </div>
    <div class="modal-body">
        <p>
            The following {{ gallery.type }}s will be deleted:
            <ul>
                {% for image in gallery.files %}
                    <li>
                        {{ image.id }}
                        {{ image.name }}
                        {{ image.contentType }}
                    </li>
                {% else %}
                    <li>No {{ gallery.type }}s in this gallery yet.</li>
                {% endfor %}
            </ul>
            When you delete this gallery, the following galleries will also be deleted:
            <ul>
                {% for child in gallery.children %}
                    <li>{% include 'KunstmaanMediaBundle:Gallery:showgallery.html.twig' %}</li>
                {% else %}
                    <li>No subgalleries in this gallery yet.</li>
                {% endfor %}
            </ul>
        </p>
    </div>
    <div class="modal-footer">
        <a href="{{ path('KunstmaanMediaBundle_gallery_delete', { 'gallery_id' : gallery.id }) }}" class="btn danger">Delete</a>
        <a onclick="javascript:$('#delete-modal').modal('hide')" href="#" class="btn secondary">Cancel</a>
    </div>
</div>