{% extends 'KunstmaanAdminBundle:Default:layout.html.twig' %}

{% block extrajavascript %}
	<script type="text/javascript">
		// Close the dialog
	    function closeme() {
	    	window.close()
	    }
	
	    // Handle click of OK button
	    function handleOK(result) {
	    	if (window.opener) {
	    	    window.opener.dialogWin.returnedValue = result;
	    		window.opener.dialogWin.returnFunc()
	    	} else {
	    		//alert("You have closed the main window.\n\nNo action will be taken on the choices in this dialog box.")
	    	}
	    	window.closeme()
	    	return false
	    }
	
	    // Handle click of Cancel button
	    function handleCancel() {
	    	window.closeme()
	    	return false
	    }
	
	    function doBodyLoad() {
	    	if (window.opener) {
	    		if (window.opener.blockEvents) {
	    			window.opener.blockEvents();
	    		}
	    	}
	    }
	
	    function doBodyUnload() {
	    	if (window.opener) {
	    		if (window.opener.unblockEvents) {
	    			window.opener.unblockEvents();
	    		}
	    	}
	    }
	</script>
{% endblock %}

{% block body %}
	<div class="container-fluid media_popup_temp">
		<aside class="sidebar">
			{% block sidebar %}
				{% include 'KunstmaanMediaBundle:Chooser:filechoosersidebar.html.twig' %}
			{% endblock %}
		</aside>
		<div class="content">
			{% block breadcrumb %}{% endblock %}
			{% block content %}{% endblock %}
		</div>
	</div>
{% endblock %}