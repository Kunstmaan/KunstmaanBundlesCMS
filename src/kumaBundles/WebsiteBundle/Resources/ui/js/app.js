var kumaBundlesWebsiteBundle = kumaBundlesWebsiteBundle || {};

kumaBundlesWebsiteBundle = (function($, window, undefined) {

    var init;

    init = function() {
    };

    return {
        init: init
    };

}(jQuery, window));

$(function() {
    kumaBundlesWebsiteBundle.init();
});
